import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    textArea: {
        width:200,
        height:100,
        borderColor: 'red',
        borderWidth:1
    }
});

export default style;